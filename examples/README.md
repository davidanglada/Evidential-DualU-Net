# Examples

After installation, run inference on an image you are licensed to use:

```bash
python scripts/infer.py --config configs/inference.yaml --checkpoint /path/to/model.pth --image /path/to/image.png --output outputs/example.npz
```

No clinical data or generated substitute is included.

